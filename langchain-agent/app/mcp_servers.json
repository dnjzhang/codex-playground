{
  "servers": {
    "playwright": {
      "enabled": true,
      "transport": "stdio",
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest", "--browser=chrome"],
      "env": {
        "NPM_CONFIG_CACHE": "/tmp/langchain-agent-npm-cache"
      }
    }
  }
}
